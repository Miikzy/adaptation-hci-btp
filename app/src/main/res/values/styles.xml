<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#6891BA</item>
        <item name="colorPrimaryDark">#14406D</item>
        <item name="colorAccent">#AD5E7B</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="primaryTextColor">#808080</item>


    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Dark base activity theme -->
    <style name="AppTheme_Dark" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorAccent">#AD5E7B</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:colorBackground">@color/background_material_light</item>
        <item name="primaryTextColor">@color/text_color_primary</item>
        <item name="android:textColor">@color/text_color_primary</item>
        <item name="android:actionMenuTextColor">@color/text_color_primary</item>

    </style>

</resources>
